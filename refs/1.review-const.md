# Object-Oriented Programming and Data Structures  
## COMP2012: Pointer, Reference, New C++11 Features, C++ Class Revision & const-ness

Cecia Chan  
Brian Mak  
Pedro Sander  

Department of Computer Science & Engineering  
The Hong Kong University of Science and Technology  
Hong Kong SAR, China  

{kccecia, bmak, psander}@ust.hk  

---

# Why Take This Course?

You have taken **COMP1021/1022P and COMP2011**. So you can program already, right?

Think about this:  
You have been learning English for many years, but can you write a novel?

You basically have learned the **C part of C++** in COMP2011 with a brief introduction to C++ classes, and you can write small C++ programs.

But what if you are going to write a **large program**, probably with a **team of programmers**?

In this course, you will learn the **essence of OOP** with some **new C++ constructs** with an aim to write **large software systems**.

---

# Part I  
## Quick Review: Reference and Pointer

---

## Variable, Reference Variable, Pointer Variable

```cpp
#include <iostream> /* File: confusion.cpp */
using namespace std;

int x = 5; // An int variable
int& xref = x; // A reference variable: xref is an alias of x
int* xptr = &x; // A pointer variable: xptr points to x

void xprint()
{
    cout << hex << endl; // Print numbers in hexadecimal format
    cout << "x = " << x << "\t\tx address = " << &x << endl;
    cout << "xref = " << xref << "\t\txref address = " << &xref << endl;
    cout << "xptr = " << xptr << "\txptr address = " << &xptr << endl;
    cout << "*xptr = " << *xptr << endl;
}

int main()
{
    x += 1; xprint();
    xref += 1; xprint();
    xptr = &xref; xprint(); // Now xptr points to xref

    return 0;
}
```

---

## Pointer vs. Reference

Reference can be thought of as a **special kind of pointer**, but there are **three big differences**:

1. A pointer can point to nothing (`nullptr`), but a reference is **always bound to an object**.

2. A pointer can point to **different objects at different times** (through assignments).  
   A reference is **always bound to the same object**.

   Assignments to a reference do **not change the object it refers to**, but only the value of the referenced object.

3. The name of a pointer refers to **the pointer itself**.  
   The `*` or `->` operators have to be used to access the underlying object.

   The name of a reference **always refers to the object itself**, and there are **no special operators**.

---

# Part II  
## Some New Features in C++11

---

## A List of New Features in C++11

- Uniform and general initialization using `{}` list  
- Type deduction of variables from initializer: `auto` *(NOT allowed in COMP2011/2012)*  
- Prevention of narrowing  
- Generalized constant expressions: `constexpr`  
- Range-based `for` statement  
- Null pointer keyword: `nullptr`  
- Scoped and strongly typed enums: `enum class`  
- Rvalue references enabling move semantics  
- Lambda expressions  
- Unicode character support  
- `long long` integer type  
- Delegating constructors  
- In-class member initializers  
- Explicit conversion operators  
- `override` and `final` keywords

---

## General Initialization Using `{}` Lists

Previously initialization usually used the assignment operator `=`.

Example:

```cpp
int x = 5;
float y = 9.8;
int& xref = x;
int a[] = {1,2,3};
```

C++11 allows a **uniform initializer** using curly braces:

```cpp
int x = {5}; // = optional
float y {9.8};
int& xref {x};
int a[] {1,2,3};
```

---

## Initializer Example 1

```cpp
#include <iostream> /* File: initializer1.cpp */
using namespace std;

int main()
{
    int w = 3.4;
    int x1 {6};
    int x2 = {8};
    const int y {'k'};
    int z {6.4}; // Error!

    cout << "w = " << w << endl;
    cout << "x1 = " << x1 << endl << "x2 = " << x2 << endl;
    cout << "y = " << y << endl << "z = " << z << endl;

    int& ww = w;
    int& www {ww};
    www = 123;

    cout << "www = " << www << endl;

    return 0;
}
```

Error example:

```
error: narrowing conversion of 6.4 from double to int
```

---

## Initializer Example 2

```cpp
#include <iostream> /* File: initializer2.cpp */
using namespace std;

int main()
{
    const char s1[] = "Steve Jobs";
    const char s2[] {"Bill Gates"};
    const char s3[] = {'h','k','u','s','t','\0'};
    const char s4[] {'h','k','u','s','t','\0'};

    cout << "s1 = " << s1 << endl;
    cout << "s2 = " << s2 << endl;
    cout << "s3 = " << s3 << endl;
    cout << "s4 = " << s4 << endl;

    return 0;
}
```

---

## Differences Between `=` and `{}` Initializers

`{}` initializer is **more restrictive**:

- It does **not allow narrowing conversions**

`{}` initializer is **more general**:

- arrays  
- aggregate structures  
- class objects  

---

## Range-for Statements

Traditional loop:

```cpp
for (int k = 0; k < 5; ++k)
    cout << k*k << endl;
```

Range-for loop:

```cpp
for (int k : {0,1,2,3,4})
    cout << k*k << endl;

for (int k : {1,19,54})
    cout << k*k << endl;
```

---

## Range-for Example

```cpp
#include <iostream> /* File: range-for.cpp */
using namespace std;

int main()
{
    cout << "Square some numbers in a list" << endl;

    for (int k : {0,1,2,3,4})
        cout << k*k << endl;

    int range[] {2,5,27,40};

    cout << "Square the numbers in range" << endl;

    for (int k : range)
        cout << k*k << endl;

    cout << "Print the numbers in range" << endl;

    for (int v : range)
        cout << v << endl;

    for (int& x : range)
        x *= 2;

    cout << "Again print the numbers in range" << endl;

    for (int v : range)
        cout << v << endl;

    return 0;
}
```

---

# Part III  
## A Revision Example: Person and Family

The system consists of a class **Person**.

A person:

- has **at most one child**
- may or may not know their **father and mother**

Family information includes:

- the person
- parents
- grandparents

---

## Expected Output

```
Name: Arthur
Father: unknown
Mother: unknown
Grand Fathers: unknown, unknown
Grand Mothers: unknown, unknown
```

(and similar outputs for other people)

---

## Person Class — Header File

```cpp
#include <iostream> /* File: person.h */
using namespace std;

class Person
{
private:
    char* _name;
    int _age;
    Person *_father, *_mother, *_child;

public:
    Person(const char* my_name, int my_age,
           Person* my_father = nullptr,
           Person* my_mother = nullptr,
           Person* my_child = nullptr);

    ~Person();

    Person* father() const;
    Person* mother() const;
    Person* child() const;

    void print_age() const;
    void print_name() const;
    void print_family() const;

    void have_child(Person* baby);
};
```

---

## Implementation File

```cpp
#include "person.h"
#include <cstring>

Person::Person(const char* my_name, int my_age,
               Person* my_father,
               Person* my_mother,
               Person* my_child)
{
    _name = new char[strlen(my_name)+1];
    strcpy(_name, my_name);

    _age = my_age;
    _father = my_father;
    _mother = my_mother;
    _child = my_child;
}

Person::~Person()
{
    delete [] _name;
}

Person* Person::father() const { return _father; }
Person* Person::mother() const { return _mother; }
Person* Person::child() const { return _child; }

void Person::have_child(Person* baby)
{
    _child = baby;
}
```

---

## Helper Function

```cpp
void print_parent(Person* parent)
{
    if (parent)
        parent->print_name();
    else
        cout << "unknown";
}
```

---

## Print Family

```cpp
void Person::print_family() const
{
    Person *f_grandfather = nullptr, *f_grandmother = nullptr;
    Person *m_grandfather = nullptr, *m_grandmother = nullptr;

    if (_father) {
        f_grandmother = _father->mother();
        f_grandfather = _father->father();
    }

    if (_mother) {
        m_grandmother = _mother->mother();
        m_grandfather = _mother->father();
    }

    cout << "Name: "; print_name(); cout << endl;

    cout << "Father: ";
    print_parent(_father); cout << endl;

    cout << "Mother: ";
    print_parent(_mother); cout << endl;

    cout << "Grand Fathers: ";
    print_parent(f_grandfather);
    cout << ", ";
    print_parent(m_grandfather);
    cout << endl;

    cout << "Grand Mothers: ";
    print_parent(f_grandmother);
    cout << ", ";
    print_parent(m_grandmother);
    cout << endl;
}
```

---

## Family Test Program

```cpp
#include "person.h"

int main()
{
    Person arthur("Arthur",65,nullptr,nullptr,nullptr);
    Person becky("Becky",63,nullptr,nullptr,nullptr);
    Person claire("Claire",32,&arthur,&becky,nullptr);
    Person eddy("Eddy",4,nullptr,&claire,nullptr);

    arthur.have_child(&claire);
    becky.have_child(&claire);
    claire.have_child(&eddy);

    arthur.print_family(); cout << endl;
    becky.print_family(); cout << endl;
    claire.print_family(); cout << endl;
    eddy.print_family(); cout << endl;

    return 0;
}
```

---

# Part IV  
## General Remarks on C++ Classes

---

## Structure vs Class

```cpp
struct Person
{
    char* _name;
    int _age;
    Person *_father, *_mother, *_child;
};
```

Equivalence:

```
struct { ... } ≡ class { public: ... }
class { ... } ≡ struct { private: ... }
```

---

## Class Name Equivalence

```cpp
class X { int a; };
class Y { int a; };

X x;
Y y;

x = y; // error
```

C++ uses **name equivalence**, not structure equivalence.

---

## Forward Declaration

```cpp
class Cell;

class List
{
    int size;
    Cell* data;
};
```

---

## Default Member Initializers (C++11)

```cpp
class Complex
{
private:
    float real = 1.3;
    float imag {0.5};
};
```

---

## Member Functions

Three ways to define:

### Inline inside class

```cpp
Person* child() const { return _child; }
```

### Inline outside class

```cpp
inline Person* Person::child() const { return _child; }
```

### In .cpp file

```cpp
Person* Person::child() const
{
    return _child;
}
```

---

## Class Scope Operator

```cpp
temperature::kelvin()
temperature::celsius()
```

---

## this Pointer

Every member function implicitly has:

```cpp
Person* this
```

Example translation:

```cpp
void Person::have_child(Person* baby)
```

becomes

```cpp
void Person::have_child(Person* this, Person* baby)
```

---

# Example: Complex Class

```cpp
class Complex
{
private:
    float real, imag;

public:

    Complex(float r, float i)
    {
        real = r;
        imag = i;
    }

    Complex add1(const Complex& x)
    {
        real += x.real;
        imag += x.imag;
        return (*this);
    }

    Complex* add2(const Complex& x)
    {
        real += x.real;
        imag += x.imag;
        return this;
    }

    Complex& add3(const Complex& x)
    {
        real += x.real;
        imag += x.imag;
        return (*this);
    }
};
```

---

# Part V  
## const-ness

---

## const

```cpp
const float PI = 3.1416;
```

Older method:

```cpp
#define PI 3.1416
```

`const` is safer and works for:

- objects
- pointers
- member functions

---

## const Member Functions

```cpp
class Date
{
public:

    void print() const
    {
        cout << year << "/" << month << "/" << day << endl;
    }
};
```

A const object **can only call const member functions**.

---

## const and Pointers

Rule:

- `const` left of `*` → object is const  
- `const` right of `*` → pointer is const

Example:

```cpp
char *const cpc = &c;
const char* pcc;
const char *const cpcc = &c;
```

---

## Pointer Example

```cpp
const char* pcc {s};
pcc = p; // allowed
pcc[5] = '5'; // error
```

---

## const References

```cpp
void cbr(int& x)
{
    x += 10;
}

void cbcr(const int& x)
{
    x += 10; // error
}
```

Advantages:

1. Prevent accidental modification
2. Accept both const and non-const arguments

---

# Good Practice Summary

Use **const** when objects should not change.

Examples:

```cpp
const double PI = 3.1415927;

const Date handover(1,7,1997);
```

For function parameters:

```cpp
void print_height(const Large_Obj& LO);
```

For member functions:

```cpp
int Date::get_day() const
{
    return day;
}
```

---

# Final Summary

### Member Function Calls

| Calling Object | const function | non-const function |
|---|---|---|
| const object | ✔ | ✘ |
| non-const object | ✔ | ✔ |

### Function Arguments

| Argument type | const reference | non-const reference |
|---|---|---|
| literal constant | ✔ | ✘ |
| const object | ✔ | ✘ |
| non-const object | ✔ | ✔ |

---